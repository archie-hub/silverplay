apiVersion: v1
kind: Service
metadata:
  name: {{ include "goldandsilverstacker.fullname" . }}
  labels:
    {{- include "goldandsilverstacker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goldandsilverstacker.selectorLabels" . | nindent 4 }}
